// AUTH ENDPOINTS — not live yet; authFns.js is mocked until a backend exists.
export const AUTH = {
  LOGIN: '/auth/login',
  SIGNUP: '/auth/signup',
  FORGOT_PASSWORD: '/auth/forgot-password',
};

// TODOS — demo resource backed by jsonplaceholder.
export const TODOS = {
  BASE: '/todos',
};
