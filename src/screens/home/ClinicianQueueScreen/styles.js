import { StyleSheet } from 'react-native';

import { colors, fonts } from '../../../theme/theme';

export const styles = StyleSheet.create({
  note: { fontSize: 11, fontFamily: fonts.bodySemiBold, color: colors.inkSoft, marginTop: 6 },
});
